1. Git clone project to a folder
2. Open projects folder and backend folder.
3. Start MYSQL server and create an empty database "gamenight".
4. Import the gamenight.sql file into the empty database and use it (use gamenight;) (password is empty)
5. Open main.py and install all requirements: pip install -r requirements.txt
6. run main.py
7. Go to Postman, set the URl to http://127.0.0.1:5000/ and test some requests.
